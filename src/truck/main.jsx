import React from 'react';
import ReactDOM from 'react-dom/client';
import TruckSite from './App';
import '../index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode><TruckSite /></React.StrictMode>,
);
